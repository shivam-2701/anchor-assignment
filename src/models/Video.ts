export interface Video {
  channelId: string;
  comment: number;
  earnings: number;
  like: number;
  subscriber: number;
  thumbnail: string;
  title: string;
  videoId: string;
  view: number;
  uploadOn: string;
  isTop?: boolean;
}
